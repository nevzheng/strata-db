[workspace]
members = ["lsm", "strata-store"]
# Parked while migrated onto the rebuilt lsm crate; re-added as each lands.
exclude = ["spec-test", "strata-cli", "strata-db", "strata-server"]
resolver = "3"
